Fixed Size Data - Functions and Programs


Exercise 11:

(define (distance_to_origin x y) (sqrt (+ (sqr x) (sqr y))))


Exercise 12:

;cvolume

(define (cvolume x) (expt x 3))


;csurface

(define (csurface x) (* 6 x))


Exercise 13:

(define (string-first str) (substring str 0 1))


Exercise 14:

(define (string-last str)
  (substring str (- (string-length str) 1) (string-length str)))
  
  
Exercise 15:

(define (==> sunny friday)
  (if (or friday (not sunny)) #true #false))


Exercise 16:

(define (image-area image) (* (image-height image)
                              (image-width image)))
                              
   
Exercise 17:

